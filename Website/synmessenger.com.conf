location = /.well-known/apple-app-site-association {
	types { }
	default_type application/json;
	try_files $uri =404;
}

location = /chat {
	return 301 /chat/;
}

location ~ ^/chat/(config(?:\.[^/]+)?\.json|index\.html|version)$ {
	add_header Cache-Control "no-cache";
}
